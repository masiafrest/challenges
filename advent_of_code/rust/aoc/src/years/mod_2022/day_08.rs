use aoc::get_input_example;

pub fn run() {
    let data: String = get_input_example(2022, 8);
    let mut lines = data.lines();

    lines.next();

    for (idx, line) in lines.enumerate() {

    }
}
